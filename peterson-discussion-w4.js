// start program

// function
function goTeam(){
    var sports = ["football", "hockey", "basketball", "soccer"];  // Here is an array of 4 elements
    sports.sort();    // Alphabetizes the elements
    for(var i = 0; i < sports.length; i++) {   // This creates a loop of the elements
      console.log(sports[i]);
    }
  }

// Output from the getFruit() function
console.log(goTeam());  // The output displays the array elements



// end program